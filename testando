from operator import attrgetter

class camiseta():
    def __init__(self,nome,cor,tamanho):
        self.nome= nome
        self.cor = cor
        self.tamanho = tamanho

def ordenacaomultipla(lista, specs):
    for key, reverse in reversed(specs):
        lista.sort(key=attrgetter(key), reverse=reverse)
    return lista

while True:
  loop = int(input("\nDigite a quantidade de alunos: \n"))
  if (loop == 0 or loop > 60):
      break
  lista = [None] * loop
  cont = 0
  while cont < loop:
    nome = input("\nDigite o nome do aluno: \n")
    entrada = input("\nDigite a cor da camiseta (branco;vermelho) e o tamanho separado por espaço (P;M;G): \n")
    aux = entrada.split(" ")
    cor = aux[0]
    tamanho = aux[1]
    b = camiseta(nome,cor,tamanho)
    lista[cont] = b
    cont += 1
  aux = ordenacaomultipla(list(lista), (('cor',False),('tamanho',True), ('nome', False)))  #P,M,G
  cont = 0
  while cont < loop:
     print(aux[cont].cor,aux[cont].tamanho,aux[cont].nome)
     cont += 1
