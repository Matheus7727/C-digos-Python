from operator import attrgetter

class camiseta():
    def __init__(self,nome,cor,tamanho):
        self.nome=nome
        self.cor = cor
        self.tamanho = tamanho

def ordenacaomultipla(lista, specs):
    for key, reverse in reversed(specs):
        lista.sort(key=attrgetter(key), reverse=reverse)
    return lista

while True:
  loop = int(input("Digite a quantidade de alunos"))
  if (loop == 0 or loop > 60):
      break
  lista = [None] * loop
  cont = 0
  while cont < loop:
    nome = input("/Digite o nome do aluno\n")
    entrada = input("/nDigite a cor da camiseta (branco;vermelho) e o tamanho separado por espaço (P;M;G): \n")
    aux = entrada.split(" ")
    cor = aux[0]
    tamanho = aux[1]
    b = camiseta(nome,cor,tamanho)
    lista[cont] = B
    cont += 1
  aux = ordenacaomultipla(list(lista), (('cor',False),('tamnho',True),('nome',False))) #P,M,G
  cont = 0
  while cont < loop:
     print(aux[cont].cor,[cont].tamanho[cont].nome)
     cont += 1
